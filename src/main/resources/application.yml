app:
  datasource:
    main:
      driver-class-name: org.postgresql.Driver
      jdbc-url: jdbc:postgresql://localhost:5432/moviesdb
      username: postgres
      password: 'parola1234'
      pool-size: 30

server:
  error:
    include-binding-errors: always
    include-message: always